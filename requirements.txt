NEW_FILE_CODE
# Web 框架
Django>=4.0,<5.0

# 环境变量管理
python-dotenv>=1.0.0

# WSGI 服务器（PythonAnywhere 使用 Passenger，不需要 gunicorn）
# gunicorn>=21.2.0

# 静态文件服务（可选，PythonAnywhere 有自己的静态文件服务）
whitenoise>=6.6.0

# 图片处理
Pillow>=10.1.0

# SQLite 支持（Django 内置，无需额外安装）
# mysqlclient 和 psycopg2-binary 可以删除，因为使用 SQLite
